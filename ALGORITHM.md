# 卡诺图自动化简算法说明

## 1. 算法目标

用户在卡诺图格子中设置三种状态：

| 取值 | 含义 |
|------|------|
| 1 | 该最小项**必须**被覆盖 |
| 0 | 该最小项**不能**被覆盖 |
| X | 无关项（don't care），覆盖与否均可 |

算法输出**最简与或表达式**（Sum of Products），优先级如下：

```
覆盖所有 1  →  项数最少  →  总文字数最少  →  去除冗余圈
```

---

## 2. 合法圈定义

设卡诺图有 $n$ 个变量（$n \in \{2,3,4,5,6\}$），共 $2^n$ 个最小项。

每个合法圈由 $n$ 位模式串 $P$ 定义，每位取值：

- `0`：该变量取反
- `1`：该变量取原值
- `-`：该变量被消去

例如 4 变量模式 `01-0` 覆盖所有满足 $Q₃=0, Q₂=1, Q₀=0$ 的最小项（$Q₁$ 任意），
共 $2^1 = 2$ 格。

合法圈必须满足：

1. 圈内**不能**包含任何 0 格
2. 圈内**至少**包含一个 1 格
3. 圈面积为 $2^k$（$k$ 为 `-` 的个数）

## 3. 全枚举

对所有 $3^n - 1$ 个模式（排除全 `-`），逐一检查合法性。

对 $n=6$，最多 $3^6-1=728$ 个候选圈，枚举开销极小。

每个合法圈 $C_i$ 记录：

| 字段 | 含义 |
|------|------|
| `term` | 模式串（如 `"01-0"`） |
| `cells` | 覆盖的全部最小项编号 |
| `ones` | 其中值为 1 的最小项 |
| `size` | 圈面积 $= 2^k$ |
| `literals` | 文字数 $= n - k$（即模式中 `0`/`1` 的个数） |

## 4. 覆盖矩阵

设有 $M$ 个 1 格 $\{m_1, m_2, \ldots, m_M\}$，枚举出 $N$ 个合法圈。

定义覆盖矩阵 $A \in \{0,1\}^{M \times N}$：

$$a_{ji} = \begin{cases} 1, & C_i \text{ 覆盖 } m_j \\ 0, & \text{否则} \end{cases}$$

使用位掩码（BigInt）表示覆盖关系：`coverMask[i]` 的 bit $j$ 为 1 当且仅当 $C_i$ 覆盖 $m_j$。

## 5. 优化模型

定义决策变量 $x_i \in \{0,1\}$，$x_i=1$ 表示选择圈 $C_i$。

### 主目标：项数最少

$$\min \sum_{i=1}^{N} x_i$$

$$\text{s.t.} \quad \sum_{i=1}^{N} a_{ji} x_i \geq 1, \quad \forall j \in [1, M]$$

$$x_i \in \{0, 1\}$$

记最少项数为 $T_{\min}$。

### 次目标：大圈优先

在 $\sum x_i = T_{\min}$ 约束下，最小化总文字数：

$$\min \sum_{i=1}^{N} L_i \cdot x_i$$

其中 $L_i$ 为圈 $C_i$ 的文字数。圈越大，$L_i$ 越小，实现大圈优先。

## 6. 求解方法

回溯搜索，圈按面积从大到小排序以加速剪枝。

剪枝条件：若当前已选圈数超过已找到的最优解圈数，或圈数相同但文字数已不低于最优解，则剪枝。

目标函数为字典序：**（项数, 总文字数）** 均最小化。

## 7. 冗余圈排除

最终解中，若删除某个圈后其余圈仍能覆盖所有 1 格，则该圈为冗余圈，予以删除。

$$\forall C_k \in \mathcal{S}: \bigcup_{C_i \in \mathcal{S}, i \neq k} \text{ones}_i = \bigcup_{C_i \in \mathcal{S}} \text{ones}_i \implies C_k \text{ 冗余}$$

## 8. 圈到乘积项的转换

对选中的圈，逐位比较模式串：

| 位值 | 操作 |
|------|------|
| `0` | 保留该变量的反变量（如 $Q₃'$） |
| `1` | 保留该变量的原变量（如 $Q₃$） |
| `-` | 该变量被消去 |

例如模式 `01-0`（$n=4$）：

| 位 | Q₃ | Q₂ | Q₁ | Q₀ |
|----|----|----|----|----|
| 值 | 0 | 1 | - | 0 |
| 结果 | $Q₃'$ | $Q₂$ | — | $Q₀'$ |

乘积项：$Q₃' \cdot Q₂ \cdot Q₀'$

## 9. 最终表达式

$$F = P_1 + P_2 + \cdots + P_{T_{\min}}$$

其中 $P_k$ 为第 $k$ 个选中圈对应的乘积项，各项以 `+`（或）连接。

## 10. 算法流程

```
输入: 卡诺图格子状态 (0/1/X)
  ↓
提取所有 1 格和 X 格
  ↓
枚举全部合法圈 (3^n - 1 个候选)
  ↓
过滤: 无 0 格 + 至少一个 1 格
  ↓
构建覆盖位掩码矩阵
  ↓
回溯搜索 → 项数最少 + 文字数最少
  ↓
去除冗余圈
  ↓
圈 → 乘积项
  ↓
输出最简与或表达式
```

## 11. 复杂度

| 变量数 | 格子数 | 候选圈数 | 搜索空间 |
|--------|--------|----------|----------|
| 4 | 16 | ≤ 80 | 极小 |
| 5 | 32 | ≤ 242 | 小 |
| 6 | 64 | ≤ 728 | 中等 |

回溯剪枝在绝大多数情况下可迅速收敛到最优解。最坏情况下退化为贪心兜底。

## 12. 与 Quine-McCluskey 的比较

| | QM | 本算法 |
|------|----|--------|
| 候选来源 | 迭代合并生成质蕴含项 | 全枚举所有合法圈 |
| 覆盖率 | 仅质蕴含项（可能遗漏最优非质蕴含项组合） | 所有合法圈（保证找到全局最优） |
| 优化方式 | 尽力覆盖，再按文字数择优 | 分两阶段精确优化 |
| 冗余处理 | 隐式（最小覆盖通常无冗余） | 显式去除 |
| 正确性 | 启发式近似 | 精确（枚举 + 0-1 IP） |

---

*算法作者：qianyuyingluo &emsp; 编程实现：DeepSeek-V4-Pro*
